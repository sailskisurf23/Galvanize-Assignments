uservar = int(input("enter integer: "))
divisors = []

for x in range(1,uservar+1):
    if uservar % x == 0:
        divisors.append(x)

if len(divisors) == 2 :
    print("prime")

else :
    print("not prime")
